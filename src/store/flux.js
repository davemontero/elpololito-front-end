const getState = ({ getStore, getActions, setStore }) => {
    return {
        store: {
            People:[{},],
            Users:[{},]
        },
        actions: {
            
        }
    };
};

export default getState;